# 🐛 Исправление команды /edit_project в личном чате

## Проблема

Команда `/edit_project` **перестала работать в личном чате** с ботом. При попытке вызвать команду ничего не происходило или выдавалась ошибка.

### Причина проблемы

В коде была **конфликтная ситуация**:

1. **ConversationHandler** для `edit_project_wizard` занимал команду `/edit_project`
2. **EditProjectCommand** ожидал работать с прямыми аргументами `/edit_project <name> <field> <value>`
3. Из-за того, что `ConversationHandler` всегда захватывал `/edit_project`, прямая команда **НИКОГДА** не вызывалась
4. При вызове `/edit_project` без аргументов или с одним аргументом wizard должен был запускаться, но при 2+ аргументах было непонятное поведение

## Решение

### 1. Изменения в `edit_project_wizard.py`

Добавлена умная логика в метод `start()`:

```python
# Проверяем количество аргументов:
if context.args and len(context.args) >= 2:
    # Это попытка использовать старый синтаксис - показываем сообщение
    # что нужно использовать wizard
    await update.effective_message.reply_text(
        "❌ Прямое редактирование через аргументы команды больше не поддерживается\\.\n\n"
        "Используйте интерактивный мастер:\n"
        "• `/edit_project` \\- выбрать проект из списка\n"
        "• `/edit_project <название>` \\- редактировать конкретный проект"
    )
    return ConversationHandler.END

# Если 1 аргумент - открываем wizard для конкретного проекта
if context.args and len(context.args) == 1:
    # ...запускаем wizard с выбранным проектом

# Если нет аргументов - показываем список проектов
else:
    # ...показываем список проектов
```

### 2. Сохранены изменения для отмены

Также сохранены изменения, которые позволяют **отменить** wizard на любом шаге с помощью `/cancel`:

- В `bot.py` убран фильтр `(~filters.COMMAND)` для текстовых состояний
- В `add_project_wizard.py` добавлена проверка на `/cancel`
- В `edit_project_wizard.py` добавлена проверка на `/cancel`
- В `release_wizard.py` добавлена проверка на `/cancel`

## Тестирование

### ✅ Тест 1: Команда без аргументов (список проектов)

```
1. В личном чате с ботом введите: /edit_project
2. Ожидаемый результат: Показывается список всех проектов с кнопками выбора
3. Выберите проект
4. Ожидаемый результат: Показывается меню редактирования полей проекта
```

### ✅ Тест 2: Команда с названием проекта

```
1. В личном чате введите: /edit_project MyApp
2. Ожидаемый результат: Сразу открывается меню редактирования для проекта MyApp
3. Выберите поле для редактирования
4. Введите новое значение
5. Нажмите "Сохранить и выйти"
6. Ожидаемый результат: "✅ Проект 'MyApp' обновлен!"
```

###✅ Тест 3: Старый синтаксис (проверка сообщения об ошибке)

```
1. Введите: /edit_project MyApp description Новое описание
2. Ожидаемый результат:
   "❌ Прямое редактирование через аргументы команды больше не поддерживается.

   Используйте интерактивный мастер:
   • /edit_project - выбрать проект из списка
   • /edit_project <название> - редактировать конкретный проект

   Пример: /edit_project MyApp"
```

### ✅ Тест 4: Отмена на любом шаге

```
1. Введите: /edit_project
2. Выберите проект
3. Выберите поле
4. Введите: /cancel
5. Ожидаемый результат: "❌ Редактирование отменено. Изменения не сохранены."
```

### ✅ Тест 5: Команда /back (возврат к меню)

```
1. Введите: /edit_project MyApp
2. Выберите поле
3. Введите: /back
4. Ожидаемый результат: Возврат к меню выбора полей
```

## Что изменилось для пользователей

### До исправления:
- ❌ Команда `/edit_project` не работала в личном чате
- ❌ Невозможно было прервать wizard на промежуточных шагах
- ❌ Непонятное поведение при разном количестве аргументов

### После исправления:
- ✅ Команда `/edit_project` работает в личном чате
- ✅ Можно прервать wizard в любой момент с помощью `/cancel`
- ✅ Чёткое поведение:
  - `/edit_project` - список проектов
  - `/edit_project MyApp` - редактирование конкретного проекта
  - `/edit_project MyApp field value` - показывает сообщение об использовании wizard

## Файлы изменений

- `python/src/easybuild_bot/bot.py` - убран фильтр `(~filters.COMMAND)`
- `python/src/easybuild_bot/handlers/edit_project_wizard.py` - добавлена умная логика и обработка `/cancel`
- `python/src/easybuild_bot/handlers/add_project_wizard.py` - добавлена обработка `/cancel`
- `python/src/easybuild_bot/handlers/release_wizard.py` - добавлена обработка `/cancel`

## Почему это работает теперь?

**ConversationHandler** всегда перехватывает команду `/edit_project` первым, но теперь:

1. Если передано **0 аргументов** → показывает список проектов
2. Если передан **1 аргумент** (название проекта) → открывает wizard для этого проекта
3. Если передано **2+ аргумента** → показывает сообщение об использовании wizard
4. В любой момент можно нажать `/cancel` → выход из wizard

Это обеспечивает **консистентное и предсказуемое поведение** команды! 🎯

## Совместимость

✅ Все существующие пользователи, которые использовали `/edit_project` получат понятное сообщение о том, как правильно использовать команду

✅ Новые пользователи будут использовать удобный интерактивный wizard

✅ Код следует принципам SOLID и KISS

