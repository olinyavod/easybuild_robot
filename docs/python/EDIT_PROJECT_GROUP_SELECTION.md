# Изменение редактирования групп в команде /edit_project

## Дата: 9 ноября 2025

## Описание изменений

Изменена логика редактирования групп при редактировании проекта. Теперь при выборе поля "Группы" пользователь видит список зарегистрированных групп в виде кнопок вместо текстового ввода.

## Бизнес-требование

**Один проект — одна группа**: Хотя технически модель проекта поддерживает несколько групп (`allowed_group_ids` — список), по бизнес-требованиям каждый проект должен быть привязан только к одной группе.

## Реализованные изменения

### 1. Новое состояние разговора

Добавлено новое состояние `SELECT_GROUP` в ConversationHandler для обработки выбора группы.

**Файлы:**
- `python/src/easybuild_bot/handlers/edit_project_wizard.py`
- `python/src/easybuild_bot/handlers/__init__.py`
- `python/src/easybuild_bot/bot.py`

### 2. Новый метод `show_group_selection`

```python
async def show_group_selection(self, update: Update, context: ContextTypes.DEFAULT_TYPE) -> int:
    """Show registered groups as buttons for selection."""
```

**Функциональность:**
- Получает все зарегистрированные группы из БД
- Отображает их в виде inline-кнопок
- Помечает текущую выбранную группу галочкой ✅
- Показывает опцию "🌍 Все группы" (пустой список `allowed_group_ids`)
- Кнопка "◀️ Назад" для возврата к меню полей

**Отображение:**
```
👥 Выбор группы для проекта

📦 Проект: MyApp
Текущая группа: Группа разработки

💡 Выберите группу, для которой будет доступен проект:
(только одна группа на проект по бизнес-требованиям)

[✅ Группа разработки]
[Тестовая группа]
[Production группа]
[🌍 Все группы]
[◀️ Назад]
```

### 3. Новый метод `handle_group_selection`

```python
async def handle_group_selection(self, update: Update, context: ContextTypes.DEFAULT_TYPE) -> int:
    """Handle group selection from buttons."""
```

**Функциональность:**
- Обрабатывает выбор группы из кнопок
- Полностью перезаписывает `allowed_group_ids` одним значением
- Если выбрано "Все группы" — устанавливает пустой список `[]`
- Сохраняет изменение в `context.user_data['edit_data']`
- Показывает подтверждающее сообщение с названием группы
- Возвращает пользователя к меню редактирования полей

### 4. Обновлённая функция `format_value`

```python
def format_value(field: str, value, storage=None) -> str:
```

**Изменения:**
- Добавлен параметр `storage` для доступа к БД
- Для `allowed_group_ids` теперь показывает название группы вместо ID
- Если группа не найдена — показывает ID
- Если список пустой — показывает "_все группы_"

**Примеры отображения:**
- `allowed_group_ids = [12345]` → "Группа разработки"
- `allowed_group_ids = []` → "_все группы_"

### 5. Удалена старая логика

Удалена обработка `allowed_group_ids` через текстовый ввод в методе `receive_value`:
- ❌ Больше не требуется вводить ID групп через запятую
- ✅ Выбор только через кнопки с зарегистрированными группами

### 6. Обновлён ConversationHandler

**До:**
```python
states={
    SELECT_PROJECT: [...],
    SELECT_FIELD: [...],
    EDIT_VALUE: [...],
}
```

**После:**
```python
states={
    SELECT_PROJECT: [CallbackQueryHandler(..., pattern="^(edit_select_|edit_cancel)")],
    SELECT_FIELD: [CallbackQueryHandler(..., pattern="^(edit_field_|edit_save|edit_cancel)")],
    EDIT_VALUE: [...],
    SELECT_GROUP: [CallbackQueryHandler(..., pattern="^(select_group_|group_back)")],
}
```

**Важно:** Паттерны сделаны более специфичными, чтобы избежать конфликтов между состояниями.

## Callback data patterns

- `select_group_<group_id>` — выбор конкретной группы по ID
- `select_group_all` — выбор опции "Все группы" (пустой список)
- `group_back` — возврат к меню редактирования полей

## Преимущества

1. **UX улучшен**: Не нужно запоминать или искать ID групп
2. **Меньше ошибок**: Невозможно ввести некорректный ID
3. **Визуальная обратная связь**: Текущая группа помечена галочкой
4. **Бизнес-логика**: Явно указано "один проект — одна группа"
5. **Согласованность**: Только зарегистрированные группы доступны для выбора

## Пример использования

1. Пользователь: `/edit_project MyApp`
2. Бот показывает меню с полями проекта
3. Пользователь нажимает "👥 Группы"
4. Бот показывает список зарегистрированных групп в виде кнопок
5. Пользователь выбирает группу
6. Бот подтверждает выбор и возвращает к меню
7. Пользователь нажимает "✅ Сохранить и выйти"
8. Изменения сохраняются: `allowed_group_ids = [<выбранная_группа>]`

## Обратная совместимость

- ✅ Модель данных не изменена (`allowed_group_ids` остаётся списком)
- ✅ Проекты с несколькими группами (если есть) корректно отображаются
- ✅ При редактировании выбирается одна группа (перезапись списка)
- ✅ Старые проекты без групп продолжают работать (пустой список)

## Затронутые файлы

1. `python/src/easybuild_bot/handlers/edit_project_wizard.py`
   - Добавлен state `SELECT_GROUP`
   - Добавлен метод `show_group_selection()`
   - Добавлен метод `handle_group_selection()`
   - Обновлена функция `format_value()` (добавлен параметр `storage`)
   - Удалена обработка `allowed_group_ids` из `receive_value()`
   - Обновлены вызовы `format_value()` с параметром `storage`

2. `python/src/easybuild_bot/handlers/__init__.py`
   - Добавлен экспорт `SELECT_GROUP`

3. `python/src/easybuild_bot/bot.py`
   - Добавлен импорт `SELECT_GROUP`
   - Обновлён ConversationHandler для `edit_project`

## Следование принципам

### SOLID
- **Single Responsibility**: Каждый метод отвечает за одну операцию
- **Open/Closed**: Расширили функциональность без изменения существующей логики других полей
- **Interface Segregation**: Не добавили лишних зависимостей
- **Dependency Inversion**: Используем Storage через интерфейс

### KISS (Keep It Simple, Stupid)
- Простой и понятный интерфейс выбора группы
- Визуальная обратная связь (галочки)
- Логика реализована минимальным количеством методов

### Удаление неиспользуемого кода
- ✅ Удалена старая логика ввода групп через текст
- ✅ Убраны неиспользуемые подсказки для `allowed_group_ids`

## Тестирование

Для проверки изменений:

1. Зарегистрируйте несколько групп через `/register_group`
2. Создайте или отредактируйте проект: `/edit_project`
3. Выберите поле "👥 Группы"
4. Убедитесь, что:
   - ✅ Отображается список зарегистрированных групп
   - ✅ Текущая группа помечена галочкой
   - ✅ При выборе группы она сохраняется в `allowed_group_ids` как `[group_id]`
   - ✅ Можно выбрать "Все группы" (пустой список)
   - ✅ Кнопка "Назад" работает корректно
   - ✅ После сохранения отображается правильное название группы

