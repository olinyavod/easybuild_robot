# 🧪 Инструкция по тестированию исправления команды /edit_project

## Что было исправлено?

Команда `/edit_project` и другие wizard'ы теперь могут быть **отменены на любом шаге** с помощью команды `/cancel`.

## ✅ Что нужно протестировать:

### 1. Тестирование /edit_project

#### Сценарий 1: Отмена при редактировании поля
```
1. Запустите: /edit_project
2. Выберите существующий проект из списка
3. Выберите любое поле для редактирования (например, "Описание")
4. Введите: /cancel
5. ✅ Ожидаемый результат: "❌ Редактирование отменено. Изменения не сохранены."
```

#### Сценарий 2: Использование /back (проверка совместимости)
```
1. Запустите: /edit_project
2. Выберите проект
3. Выберите поле
4. Введите: /back
5. ✅ Ожидаемый результат: Возврат к меню выбора полей
```

#### Сценарий 3: Успешное редактирование
```
1. Запустите: /edit_project
2. Выберите проект
3. Выберите поле "Описание"
4. Введите новое описание
5. Нажмите "Сохранить и выйти"
6. ✅ Ожидаемый результат: "✅ Проект '<название>' обновлен!"
```

### 2. Тестирование /add_project

#### Сценарий 1: Отмена на шаге ввода названия
```
1. Запустите: /add_project
2. Введите любое название проекта
3. Введите: /cancel
4. ✅ Ожидаемый результат: "❌ Создание проекта отменено."
```

#### Сценарий 2: Отмена на шаге ввода Git URL
```
1. Запустите: /add_project
2. Введите название: TestProject
3. Выберите тип проекта (например, Flutter)
4. Введите: /cancel
5. ✅ Ожидаемый результат: "❌ Создание проекта отменено."
```

#### Сценарий 3: Отмена на любом другом шаге
```
Попробуйте ввести /cancel на шагах:
- Путь к файлу проекта
- Ветка разработки
- Ветка релиза

✅ Везде должна работать отмена
```

### 3. Проверка информационных сообщений

Убедитесь, что в сообщениях wizard'ов есть подсказки:
- `/add_project`: "ℹ️ Используйте /cancel для отмены в любой момент."
- `/edit_project`: "ℹ️ Используйте /cancel для отмены всех изменений."

## 🐛 Если что-то не работает:

### Проблема: Команда /cancel не работает
**Возможные причины:**
1. Бот не перезапущен после изменений
2. В ConversationHandler используется старый фильтр `(~filters.COMMAND)`

**Решение:**
```bash
# Перезапустите бота
# Проверьте, что изменения применены
cd /home/olinyavod/projects/easybuild_bot
git diff python/src/easybuild_bot/bot.py
```

### Проблема: Команда /edit_project вообще не запускается
**Возможные причины:**
1. Нет прав администратора
2. Проблема с доступом к базе данных

**Решение:**
```bash
# Проверьте логи бота
tail -f /path/to/bot/logs/easybuild_bot.log

# Убедитесь, что у вас есть права администратора
# Выполните в личном чате с ботом: /admin
```

## 📋 Checklist для полного тестирования:

- [ ] `/edit_project` - отмена при редактировании поля
- [ ] `/edit_project` - команда `/back` работает
- [ ] `/edit_project` - успешное редактирование и сохранение
- [ ] `/add_project` - отмена на шаге "Название"
- [ ] `/add_project` - отмена на шаге "Git URL"
- [ ] `/add_project` - отмена на других шагах
- [ ] Информационные сообщения отображаются корректно
- [ ] Нет ошибок в логах бота

## 🎯 Ожидаемое поведение:

✅ **До исправления:** Команда `/cancel` игнорировалась в состояниях текстового ввода

✅ **После исправления:** Команда `/cancel` работает на **любом шаге** любого wizard'а

## 🔧 Технические детали:

### Измененные файлы:
1. `python/src/easybuild_bot/bot.py` - убран фильтр `(~filters.COMMAND)`
2. `python/src/easybuild_bot/handlers/add_project_wizard.py` - добавлена обработка `/cancel`
3. `python/src/easybuild_bot/handlers/edit_project_wizard.py` - добавлена обработка `/cancel`
4. `python/src/easybuild_bot/handlers/release_wizard.py` - добавлена обработка `/cancel`

### Почему это работает:

**Раньше:**
```python
# Фильтр исключал все команды, включая /cancel
MessageHandler(filters.TEXT & (~filters.COMMAND), handler)
```

**Теперь:**
```python
# Принимаются все текстовые сообщения, включая команды
MessageHandler(filters.TEXT, handler)

# В каждом обработчике проверяем /cancel вручную
if text == "/cancel":
    return await self.cancel(update, context)
```

Это даёт больше контроля и гибкости!

