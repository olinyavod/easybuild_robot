# 🔍 Линтинг и проверка качества кода

## Обзор

В проекте настроены современные инструменты для проверки качества кода, следующие принципам **SOLID** и **KISS**.

## Python проект

### 🛠️ Инструменты

1. **Ruff** - быстрый линтер и форматтер (замена flake8, black, isort)
2. **MyPy** - проверка типов
3. **Pre-commit** - автоматические проверки перед коммитом

### 📦 Установка

```bash
cd python
pip install -r requirements-dev.txt
```

### 🚀 Использование

#### Проверка кода

```bash
cd python
./lint.sh
```

Этот скрипт запустит:
- Ruff линтер (проверка стиля и потенциальных ошибок)
- Ruff форматтер (проверка форматирования)
- MyPy (проверка типов)

#### Автоматическое исправление

```bash
cd python
./fix.sh
```

Автоматически исправит:
- Проблемы со стилем кода
- Сортировку импортов
- Форматирование

#### Ручные команды

```bash
# Проверка линтером
ruff check .

# Автоисправление
ruff check --fix .

# Форматирование
ruff format .

# Проверка типов
mypy src/
```

### 🔧 Pre-commit хуки

Для автоматической проверки перед каждым коммитом:

```bash
# Установка (в корне проекта)
pre-commit install

# Ручной запуск
pre-commit run --all-files
```

### ⚙️ Конфигурация

Настройки находятся в `python/pyproject.toml`:

- **Длина строки**: 100 символов
- **Включенные правила**: E, W, F, I, N, UP, B, C4, DTZ, и другие
- **Проверка типов**: частичная (для постепенного улучшения)

### 📝 Основные правила

#### Именование
- `snake_case` для функций и переменных
- `PascalCase` для классов
- `UPPER_CASE` для констант

#### Импорты
```python
# Стандартная библиотека
import os
import sys

# Сторонние библиотеки
from telegram import Update

# Локальные импорты
from easybuild_bot.models import Project
```

#### Типы
```python
def process_project(project_id: int, name: str) -> bool:
    """Обрабатывает проект."""
    ...
```

#### Документация (Google Style)
```python
def build_apk(project: Project, version: str) -> str:
    """Собирает APK файл для проекта.

    Args:
        project: Проект для сборки
        version: Версия для сборки

    Returns:
        Путь к собранному APK файлу

    Raises:
        BuildError: Если сборка не удалась
    """
    ...
```

## Dart проект

### 🛠️ Инструменты

Встроенный анализатор Dart с расширенными правилами.

### 🚀 Использование

```bash
cd dart

# Анализ кода
dart analyze

# Исправление проблем
dart fix --apply
```

### ⚙️ Конфигурация

Настройки в `dart/analysis_options.yaml`:

- Строгий режим типизации
- Предпочтение `const` конструкторов
- Обязательная документация публичных API
- Предпочтение одинарных кавычек

### 📝 Основные правила

#### Стиль
```dart
// ✅ Хорошо
const MyWidget({super.key});

// ❌ Плохо
MyWidget({Key? key}) : super(key: key);
```

#### Неизменяемость
```dart
// ✅ Хорошо
final userName = 'John';
const maxRetries = 3;

// ❌ Плохо
var userName = 'John';
```

## 🎯 Цели линтинга

### SOLID принципы
- **Single Responsibility**: функции и классы делают одну вещь
- **Open/Closed**: код открыт для расширения, закрыт для изменения
- **Liskov Substitution**: подклассы заменяемы базовыми классами
- **Interface Segregation**: много специфичных интерфейсов лучше одного общего
- **Dependency Inversion**: зависимость от абстракций, а не реализаций

### KISS (Keep It Simple, Stupid)
- Читаемый и понятный код
- Избегание излишней сложности
- Простые и прямые решения

## 📊 Интеграция с IDE

### VS Code

Установите расширения:
- Python: `ms-python.python`
- Ruff: `charliermarsh.ruff`
- MyPy: включен в Python расширении
- Dart: `Dart-Code.dart-code`

Настройки в `.vscode/settings.json`:
```json
{
  "[python]": {
    "editor.formatOnSave": true,
    "editor.codeActionsOnSave": {
      "source.fixAll": true,
      "source.organizeImports": true
    },
    "editor.defaultFormatter": "charliermarsh.ruff"
  },
  "[dart]": {
    "editor.formatOnSave": true,
    "editor.codeActionsOnSave": {
      "source.fixAll": true
    }
  }
}
```

### PyCharm / IntelliJ IDEA

1. Settings → Tools → External Tools
2. Добавьте Ruff и MyPy как внешние инструменты
3. Settings → Editor → Inspections
4. Включите все Python инспекции

## ❓ FAQ

### Q: Как временно отключить правило?

**Python:**
```python
# ruff: noqa: E501
very_long_line = "..."

# Или для блока
# ruff: noqa
def legacy_code():
    ...
```

**Dart:**
```dart
// ignore: prefer_const_constructors
Widget build(BuildContext context) { ... }
```

### Q: Как добавить новое правило?

Отредактируйте:
- Python: `python/pyproject.toml` → `[tool.ruff.lint]`
- Dart: `dart/analysis_options.yaml` → `linter.rules`

### Q: CI/CD интеграция?

Добавьте в ваш CI pipeline:

```yaml
# .github/workflows/lint.yml
- name: Lint Python
  run: |
    cd python
    pip install -r requirements-dev.txt
    ./lint.sh

- name: Lint Dart
  run: |
    cd dart
    dart analyze
```

## 🔗 Полезные ссылки

- [Ruff документация](https://docs.astral.sh/ruff/)
- [MyPy документация](https://mypy.readthedocs.io/)
- [Dart lints](https://dart.dev/guides/language/analysis-options)
- [Pre-commit](https://pre-commit.com/)




