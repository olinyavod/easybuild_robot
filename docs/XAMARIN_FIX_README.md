# Исправление версионирования Xamarin проектов

## 📋 Краткое описание

**Проблема**: Бот не мог найти версию в проектах Xamarin, если `project_file_path` указывал на `.sln` файл.

**Решение**: Бот теперь автоматически ищет версию во **всех** `.csproj` файлах в папке проекта и подпапках.

## ✅ Что было сделано

### 1. Обновлена логика поиска версий
- ✅ Добавлен метод `_find_all_csproj_files()` для поиска всех `.csproj` файлов
- ✅ Метод `get_current_version()` теперь ищет версию во всех найденных файлах
- ✅ Метод `update_version()` обновляет версию во всех `.csproj` файлах

### 2. Улучшены сообщения об ошибках
- ✅ Для Xamarin проектов показывается специфичное сообщение
- ✅ Объясняется, где именно ищется версия
- ✅ Даются примеры тегов версий

### 3. Добавлена документация
- ✅ `XAMARIN_VERSION_FIX.md` — подробное описание проблемы и решения
- ✅ `XAMARIN_VERSION_SUMMARY.md` — резюме изменений
- ✅ `CHANGELOG_2025_11_09.md` — краткий обзор
- ✅ Тестовый скрипт для проверки работы

## 🎯 Как использовать

### Для существующих проектов

1. **Добавьте тег версии** в любой `.csproj` файл проекта:

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Version>1.0.0</Version>  ← Добавьте эту строку
  </PropertyGroup>
</Project>
```

2. **Закоммитьте и запушьте** изменения:

```bash
git add .
git commit -m "Добавлена версия в .csproj"
git push
```

3. **Запустите сборку** через бота

Бот автоматически найдет версию!

## 📦 Поддерживаемые теги версий

Бот ищет следующие теги в файлах `.csproj`:

| Тег | Описание |
|-----|----------|
| `<Version>` | Стандартный тег версии .NET |
| `<ApplicationVersion>` | Версия приложения |
| `<ApplicationDisplayVersion>` | Отображаемая версия (MAUI) |

Достаточно добавить **один** из этих тегов.

## 🧪 Тестирование

Для проверки работы используйте тестовый скрипт:

```bash
cd /home/olinyavod/projects/easybuild_bot
python3 python/scripts/test_xamarin_version_fix.py
```

**Важно**: Измените путь к проекту в скрипте перед запуском!

## 📚 Дополнительная информация

- **Подробная документация**: `docs/XAMARIN_VERSION_FIX.md`
- **Резюме изменений**: `docs/XAMARIN_VERSION_SUMMARY.md`
- **Changelog**: `docs/CHANGELOG_2025_11_09.md`

## 🔧 Технические детали

### Изменённые файлы

1. `python/src/easybuild_bot/version_services/xamarin_version_service.py`
   - Добавлен метод `_find_all_csproj_files()`
   - Обновлен метод `get_current_version()`
   - Обновлен метод `update_version()`

2. `python/src/easybuild_bot/commands/implementations/prepare_release_callback.py`
   - Улучшены сообщения об ошибках для Xamarin

### Принципы

- ✅ **SOLID**: Разделение ответственности
- ✅ **KISS**: Простая логика
- ✅ **DRY**: Переиспользуемый код
- ✅ Чистый и поддерживаемый код

## 🚀 Быстрый старт

**Для нового проекта Xamarin:**

1. Создайте или откройте `.csproj` файл
2. Добавьте в секцию `<PropertyGroup>`:
   ```xml
   <Version>1.0.0</Version>
   ```
3. Сохраните, закоммитьте и запушьте
4. Запустите сборку через бота

Готово! 🎉

## ❓ Частые вопросы

**Q: Нужно ли менять `project_file_path` с `.sln` на `.csproj`?**
A: Нет! Бот теперь автоматически найдет все `.csproj` файлы.

**Q: В каких файлах нужно добавить версию?**
A: Достаточно добавить в **один** `.csproj` файл (в любой).

**Q: Какие теги версий поддерживаются?**
A: `<Version>`, `<ApplicationVersion>` или `<ApplicationDisplayVersion>`.

**Q: Бот обновляет версию во всех `.csproj` файлах?**
A: Да, при подготовке релиза бот обновит версию во всех найденных `.csproj` файлах.

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь, что версия указана в правильном формате (`X.Y.Z`)
3. Проверьте, что файл `.csproj` содержит тег версии в секции `<PropertyGroup>`

---

**Дата**: 9 ноября 2025
**Автор**: EasyBuild Bot Team
