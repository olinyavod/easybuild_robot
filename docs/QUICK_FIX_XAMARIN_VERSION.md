# 🔧 Быстрое решение: "Не удалось определить версию Xamarin проекта"

## 🎯 Проблема

Бот выдаёт ошибку при попытке создать релиз:
```
❌ Не удалось определить текущую версию проекта Fintech
```

## ✅ Решение за 3 шага

### Шаг 1: Проверьте структуру проекта

```bash
cd /home/olinyavod/projects/easybuild_bot/python
python3 scripts/check_xamarin_structure.py repos/fintech
```

Скрипт покажет, какие файлы найдены и есть ли в них теги версий.

### Шаг 2: Определите причину

**Возможные причины:**

1. **Репозиторий не клонирован**
   - Решение: запустите первую сборку проекта, репозиторий клонируется автоматически

2. **Файлы названы неправильно**
   - Нужно: `Something.Android.csproj` и `Something.iOS.csproj`
   - Найдено: `Something.csproj`
   - Решение: переименуйте файлы или создайте правильные

3. **Отсутствуют теги версий**
   - Решение: добавьте теги (см. Шаг 3)

### Шаг 3: Добавьте теги версий

Откройте файлы проектов и добавьте теги:

#### Для Android (`*.Android.csproj`):

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0-android</TargetFramework>
    <!-- Добавьте эти 2 строки -->
    <ApplicationVersion>1.0.0</ApplicationVersion>
    <AndroidVersionCode>10000</AndroidVersionCode>
  </PropertyGroup>
</Project>
```

#### Для iOS (`*.iOS.csproj`):

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0-ios</TargetFramework>
    <!-- Добавьте эти 2 строки -->
    <ApplicationVersion>1.0.0</ApplicationVersion>
    <CFBundleVersion>1.0.0</CFBundleVersion>
  </PropertyGroup>
</Project>
```

### Шаг 4: Зафиксируйте изменения

```bash
cd /home/olinyavod/projects/easybuild_bot/repos/fintech
git add .
git commit -m "Добавлены теги версий для Xamarin"
git push origin master
```

### Шаг 5: Попробуйте снова

Запустите создание релиза через бота. Теперь версия должна определиться успешно! ✅

## 📋 Примечания

- Версия должна быть в формате `X.Y.Z` (например, `1.0.0`)
- `AndroidVersionCode` автоматически вычисляется: `1.0.0` → `10000`, `1.2.3` → `10203`
- Обновление версии происходит автоматически при создании релиза
- Теги должны быть внутри `<PropertyGroup>` в корне проекта

## 🔍 Диагностика

Если проблема остаётся, проверьте логи бота:

```bash
tail -100 /home/olinyavod/projects/easybuild_bot/python/bot.log | grep -A 20 "Определение версии"
```

Логи покажут подробную информацию о том, какие файлы проверялись и что в них найдено.

## 📚 Подробная документация

См. `docs/XAMARIN_VERSION_LOGGING_FIX.md` для полного описания изменений.


