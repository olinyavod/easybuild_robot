## Быстрая инструкция: Исправление версионирования Xamarin (09.11.2025)

### Проблема
```
❌ Не удалось определить текущую версию проекта Fintech
Ветка релиза: master
Файл проекта: Fintech/Fintech.sln
```

### Решение (1 минута)

**Шаг 1**: Откройте **любой** `.csproj` файл в проекте

**Шаг 2**: Добавьте в секцию `<PropertyGroup>`:
```xml
<Version>1.0.0</Version>
```

**Шаг 3**: Сохраните, закоммитьте, запушьте

**Шаг 4**: Запустите сборку через бота ✅

### Пример

**До** (не работает):
```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>
</Project>
```

**После** (работает):
```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Version>1.0.0</Version>  ← Добавили эту строку
  </PropertyGroup>
</Project>
```

### Альтернативные теги
Можете использовать любой из этих тегов:
- `<Version>1.0.0</Version>`
- `<ApplicationVersion>1.0.0</ApplicationVersion>`
- `<ApplicationDisplayVersion>1.0.0</ApplicationDisplayVersion>`

### Что изменилось
✅ Бот теперь ищет версию во **всех** `.csproj` файлах
✅ Не требуется менять `project_file_path` с `.sln` на `.csproj`
✅ Работает с любой структурой проекта Xamarin

### Документация
- Подробно: `docs/XAMARIN_VERSION_FIX.md`
- README: `docs/XAMARIN_FIX_README.md`
- Тест: `python/scripts/test_xamarin_version_fix.py`

---

**Готово!** Больше никаких проблем с версионированием Xamarin проектов! 🎉
