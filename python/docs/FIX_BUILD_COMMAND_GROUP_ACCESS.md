# Исправление: Команда сборки доступна в группах

## Дата: 9 ноября 2025

## Проблема

При отправке текстовой команды "собери проект" в группе бот отвечал:
```
Эта команда доступна только в личном чате с ботом
```

Хотя команда `/build` должна быть доступна как в личных чатах, так и в группах.

## Причина

Система семантического распознавания команд распознавала фразу **"собери проект"** как команду **`/add_project`** (создать новый проект) вместо **`/build`** (запустить сборку).

Это происходило потому, что:
1. `AddProjectCommand` имела семантический тег **"создать проект"**
2. `EditProjectCommand` имела семантический тег **"редактировать проект"**
3. Эти команды имеют уровень доступа **ADMIN** (доступны только в личном чате)
4. Слово "проект" в тегах создавало конфликт с фразой "собери проект"

## Решение

### 1. Добавлено имя команды в сообщение об ошибке

**Файл:** `python/src/easybuild_bot/commands/base.py`

**Было:**
```python
return False, "Эта команда доступна только в личном чате с ботом"
```

**Стало:**
```python
return False, f"Команда {self.get_command_name()} доступна только в личном чате с ботом"
```

Теперь пользователь видит, какая именно команда была распознана неправильно.

### 2. Улучшено логирование распознавания команд

**Файл:** `python/src/easybuild_bot/commands/registry.py`

Добавлено логирование уровня доступа распознанной команды:
```python
logger.info(f"✓ Match: '{text}' -> {best_match} (similarity: {best_score:.3f}, access_level: {best_command.get_access_level().value}, params: {params})")
```

### 3. Сделаны более специфичными семантические теги админских команд

**Файл:** `python/src/easybuild_bot/commands/implementations/add_project_command.py`

**Было:**
```python
return [
    "добавить проект",
    "создать проект",
    "новый проект",
    "добавление проекта"
]
```

**Стало:**
```python
return [
    "добавить новый проект",
    "создать новый проект",
    "зарегистрировать новый проект",
    "добавление нового проекта"
]
```

**Файл:** `python/src/easybuild_bot/commands/implementations/edit_project_command.py`

**Было:**
```python
return [
    "редактировать проект",
    "изменить проект",
    "настроить проект",
    "обновить проект"
]
```

**Стало:**
```python
return [
    "редактировать настройки проекта",
    "изменить настройки проекта",
    "настроить конфигурацию проекта",
    "обновить параметры проекта"
]
```

**Файл:** `python/src/easybuild_bot/commands/implementations/delete_project_command.py`

**Было:**
```python
return [
    "удалить проект",
    "удаление проекта",
    "стереть проект",
    "убрать проект"
]
```

**Стало:**
```python
return [
    "удалить проект из списка",
    "удаление проекта из базы",
    "стереть проект полностью",
    "убрать проект из системы"
]
```

### 4. Расширены семантические теги команды `/build`

**Файл:** `python/src/easybuild_bot/commands/implementations/build_command.py`

Добавлены теги для лучшего распознавания фраз типа "собери проект":
```python
return [
    "сборка",
    "сборки",
    "покажи сборки",
    "выбрать сборку",
    "собрать",
    "собери",          # новое
    "построй",         # новое
    "билд",
    "собрать apk",
    "сборка приложения",
    "создать сборку",
    "собери проект",   # новое
    "построй проект",  # новое
    "сборка проекта"   # новое
]
```

## Результат

Теперь:
1. Фраза **"собери проект"** корректно распознается как команда `/build`
2. Команда `/build` доступна как в личных чатах, так и в группах
3. При ошибке распознавания пользователь видит имя команды, которая была распознана
4. Админские команды требуют более специфичных фраз для активации

## Тестирование

### В группе:
```
Пользователь: @easy_build_robot собери проект
Бот: 🔨 Выберите проект для сборки (для этой группы):
     [🦋 ChecklistApp]
     [🔷 TechnouprApp.Client]
```

### В личном чате:
```
Пользователь: собери проект
Бот: 🔨 Выберите проект для сборки (все доступные):
     [🦋 ChecklistApp]
     [🔷 TechnouprApp.Client]
     [🦋 White Broker]
```

## Файлы изменены

1. `python/src/easybuild_bot/commands/base.py`
2. `python/src/easybuild_bot/commands/registry.py`
3. `python/src/easybuild_bot/commands/implementations/build_command.py`
4. `python/src/easybuild_bot/commands/implementations/add_project_command.py`
5. `python/src/easybuild_bot/commands/implementations/edit_project_command.py`
6. `python/src/easybuild_bot/commands/implementations/delete_project_command.py`

## Рекомендации

После развертывания изменений:
1. Перезапустите бота
2. Очистите Python кеш (`.pyc` файлы)
3. Протестируйте команды в группах и личных чатах
