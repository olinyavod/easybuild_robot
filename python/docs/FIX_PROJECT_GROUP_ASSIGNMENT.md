# Решение проблемы: Команда /build показывала все проекты в группах

## Дата: 9 ноября 2025

## Проблема

Команда `/build`, вызванная из группы, показывала **все проекты** вместо проектов, привязанных только к этой группе.

## Причина

У проектов в базе данных был **пустой список** `allowed_group_ids = []`, что означает "доступен для всех групп":

```python
# В Storage.get_projects_for_group:
if not allowed_groups or group_id in allowed_groups:
    result.append(self._doc_to_project(doc))
```

### Проекты с проблемой:

1. **TechnouprApp.Client** - `allowed_group_ids: []` → показывался везде
2. **White Broker** - `allowed_group_ids: []` → показывался везде

### Правильно настроенный проект:

**ChecklistApp** - `allowed_group_ids: [-4950733491]` → показывался только в "Test Group"

## Решение

Привязали проекты к конкретным группам:

```
ChecklistApp       → Test Group (ID: -4950733491)
TechnouprApp.Client → Auto line. Checklist (ID: -4907156243)
White Broker       → Domyland platform (ID: -935064553)
```

### Скрипт для привязки:

```python
# scripts/assign_projects_to_groups.py
from src.easybuild_bot.storage import Storage
import os

data_dir = os.path.join(os.getcwd(), 'data')
storage = Storage(data_dir)

# TechnouprApp.Client → Auto line. Checklist
project1 = storage.get_project_by_name("TechnouprApp.Client")
project1.allowed_group_ids = [-4907156243]
storage.add_project(project1)

# White Broker → Domyland platform
project2 = storage.get_project_by_name("White Broker")
project2.allowed_group_ids = [-935064553]
storage.add_project(project2)
```

## Проверка результата

После привязки фильтрация работает правильно:

### Группа "Test Group" (ID: -4950733491)
```
/build → Показывает только:
  • ChecklistApp
```

### Группа "Auto line. Checklist" (ID: -4907156243)
```
/build → Показывает только:
  • TechnouprApp.Client
```

### Группа "Domyland platform" (ID: -935064553)
```
/build → Показывает только:
  • White Broker
```

### Группа "Fin tech" (ID: -602801882)
```
/build → Нет проектов
```

### Личный чат
```
/build → Показывает все проекты:
  • ChecklistApp
  • TechnouprApp.Client
  • White Broker
```

## Команды для проверки данных

### Проверить текущую конфигурацию:

```bash
cd /home/olinyavod/projects/easybuild_bot/python
source .venv/bin/activate
python3 scripts/check_project_groups.py
```

### Привязать проекты к группам:

**Вариант 1: Через Telegram бот (рекомендуется)**
1. `/edit_project`
2. Выбрать проект
3. Выбрать поле "👥 Группы"
4. Выбрать группу
5. Сохранить

**Вариант 2: Через Python скрипт**
```bash
cd /home/olinyavod/projects/easybuild_bot/python
source .venv/bin/activate
python3 scripts/assign_projects_to_groups.py
```

## Важные моменты

### Логика фильтрации

```python
def get_projects_for_group(self, group_id: int) -> List[Project]:
    """Get all projects available for a specific group."""
    for doc in projects.find({}):
        allowed_groups = doc.get("allowed_group_ids", [])
        # Если список пустой - доступен для всех групп
        # Если group_id в списке - доступен для этой группы
        if not allowed_groups or group_id in allowed_groups:
            result.append(self._doc_to_project(doc))
    return result
```

### Варианты значений allowed_group_ids:

1. `[]` (пустой список) → **Доступен для ВСЕХ групп**
2. `[group_id]` → Доступен только для указанной группы
3. `[group_id1, group_id2, ...]` → Доступен для нескольких групп

### Рекомендация:

Всегда привязывайте проекты к конкретным группам, чтобы обеспечить изоляцию данных.

## Затронутые файлы

1. `python/scripts/check_project_groups.py` — скрипт для проверки конфигурации
2. `python/scripts/assign_projects_to_groups.py` — скрипт для привязки проектов
3. База данных: обновлены поля `allowed_group_ids` для проектов

## Результат

✅ Теперь команда `/build` корректно фильтрует проекты по группам
✅ В каждой группе показываются только привязанные к ней проекты
✅ В личном чате показываются все проекты

## Тестирование

1. Зайдите в группу "Test Group"
2. Выполните `/build`
3. ✅ Должен показаться только "ChecklistApp"

4. Зайдите в группу "Auto line. Checklist"
5. Выполните `/build`
6. ✅ Должен показаться только "TechnouprApp.Client"

7. Зайдите в группу "Domyland platform"
8. Выполните `/build`
9. ✅ Должен показаться только "White Broker"

10. Откройте личный чат с ботом
11. Выполните `/build`
12. ✅ Должны показаться все 3 проекта


