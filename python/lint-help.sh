#!/bin/bash
# Быстрая справка по командам линтинга

cat << 'EOF'
╔═══════════════════════════════════════════════════════════════╗
║                   🔍 ШПАРГАЛКА ПО ЛИНТИНГУ                    ║
╚═══════════════════════════════════════════════════════════════╝

📦 УСТАНОВКА
──────────────────────────────────────────────────────────────
  cd python && pip install -r requirements-dev.txt
  pre-commit install  # Установка git хуков

🚀 БЫСТРЫЕ КОМАНДЫ
──────────────────────────────────────────────────────────────
  cd python

  ./lint.sh          # Полная проверка кода
  ./fix.sh           # Автоматическое исправление

  ruff check .       # Только проверка
  ruff check --fix . # Проверка + исправление
  ruff format .      # Форматирование кода

  mypy src/          # Проверка типов

🎯 PYTHON ПРАВИЛА
──────────────────────────────────────────────────────────────
  Длина строки:      100 символов
  Отступы:           4 пробела
  Импорты:           Автосортировка
  Типизация:         Частичная (рекомендуется)

📝 ПРИМЕР КОДА
──────────────────────────────────────────────────────────────
  from typing import Optional

  def process_data(
      user_id: int,
      name: str,
      email: Optional[str] = None
  ) -> bool:
      """Обрабатывает данные пользователя.

      Args:
          user_id: Идентификатор пользователя
          name: Имя пользователя
          email: Email (опционально)

      Returns:
          True если обработка успешна
      """
      # Реализация
      return True

🎨 DART ПРАВИЛА
──────────────────────────────────────────────────────────────
  cd dart

  dart analyze       # Анализ кода
  dart fix --apply   # Автоисправление
  dart format .      # Форматирование

  Длина строки:      80 символов
  Отступы:           2 пробела
  Кавычки:           Одинарные ' '
  Неизменяемость:    Предпочтение const/final

🔧 ОТКЛЮЧЕНИЕ ПРАВИЛ
──────────────────────────────────────────────────────────────
  Python:
    # ruff: noqa: E501
    long_line = "very long string..."

  Dart:
    // ignore: prefer_const_constructors
    Widget build() { ... }

🎁 ПОЛЕЗНОЕ
──────────────────────────────────────────────────────────────
  pre-commit run --all-files    # Проверка всех файлов
  git config --global core.editor nano  # Редактор по умолчанию

📚 ДОКУМЕНТАЦИЯ
──────────────────────────────────────────────────────────────
  См. LINTING.md для подробностей

╚═══════════════════════════════════════════════════════════════╝
EOF






