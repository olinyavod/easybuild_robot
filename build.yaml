# Конфигурация для build_runner
targets:
  $default:
    builders:
      injectable_generator:injectable_builder:
        enabled: true
        options:
          auto_register: true
          # Генерировать только для lib/
          generate_for:
            - lib/**

